#!/usr/bin/python3
"""A function that checks whether an object is an instsnce
of a class inherited from."""


def is_kind_of_class(obj, a_class):
    """
        Return: True if isintance
                otherwise False
    """
    return (isinstance(obj, a_class))
